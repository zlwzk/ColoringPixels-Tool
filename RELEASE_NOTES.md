欢迎来到 **Coloring Pixels Tool V2.2.6**！

这一版专门修「人工辅助」：**不再动不动就喊「检测到人工操作」**，速度也终于和设置一致了。

## 🐭 不再误报「人工介入」（重要）

之前的判定方式是拿**下一个扫描点**当基准：只要光标不在那个点上，就认为你动了鼠标。
于是这几种「你明明没碰鼠标」的情况都会立刻暂停：

- 刚点「开始/单行测试」——鼠标还停在面板按钮上，离画布老远；
- 每扫完一行——下一行的起点离上一行末尾差着一整行；
- 换色暂停、手动暂停之后点「继续」——鼠标还停在「继续」按钮上。

现在的判定改成：**以「引擎上一帧把光标命令到了哪里」为基准**。

SendInput 是异步的，刚发完指令就去读光标坐标往往读到的是上一帧的位置，
所以基准必须记「命令位置」而不是「实际位置」；偏差超过「干预判定半径」才算你真的把鼠标拖走了。

另外：

- 刚开始 / 刚继续 / 换色后，会把光标当前位置**重新认作基准**——鼠标停在按钮上不会被算成干预；
- 阈值会自动留出一帧的行程，高速扫描或掉帧时也不会误报。

真的自己拖鼠标时照旧会暂停（提示改成了「检测到鼠标被拖动，已暂停」），点「继续」即可接着涂。

## ⚡ 扫描速度终于等于设置值

旧实现**一帧只推进一个采样点**，步长 4px、60fps 时实际最快只有 240px/s，
面板上选 1400 也没用（选 12000 也没用），这就是为什么「感觉辅助跑得很慢」。

现在按**像素预算**推进：一帧该走多少像素就走多少像素，设置多少就是多少。
游戏自己对拖动轨迹有插值（`ClickTest.ManageInterpolate` + 自适应 `INTERPOLATE_MAX`），
所以一帧跨过好几个格子也不会漏涂。

## ✏️ 不会再画出多余的线

- **从鼠标当前位置滑到画布起点**的这段路以前是一路按住左键的，会沿路画一道；
  现在**到了第一个扫描点才按住左键**；
- **换行**时先松手：非蛇形扫描时下一行起点横跨整张画布，按着左键过去就是一条横线；
  到了新行的第一个点会自动重新按住，不掉色。

## 🎮 安装器不会再开好几个游戏窗口

Unity 游戏不拦多开，所以以前「安装完成后自动启动」+ 顺手点一下「启动游戏」，
就会出现**装一次、游戏画面冒出好几遍**的情况。

现在：

- 启动前先查一遍，**已经在运行就不再起第二个进程**，只把已有窗口切到前台；
- 游戏起来后，「启动游戏」按钮会变成**「游戏运行中」**（再点只是切窗口，不会多开）；
- 独立助手同样处理，不会再弹一次「已经在运行」；
- 安装 / 更新进行中点击「启动游戏」无效（那会儿文件正在被替换）。

## 🔧 顺手修好的

- 每行的 `CurrentRow` 现在在换行停顿/换色时也会更新，覆盖层的「当前行高亮」不会再晚一行。
