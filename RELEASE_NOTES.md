欢迎回来，**Coloring Pixels Tool V2.3.8**！

这次专门收拾「面板调不出来」这件事——原因有两处，一并修掉了。

## 修复：弹窗压住面板、热键又把面板一起关掉

- 首次启动会弹「功能总览 / 新手指引」，之前它们画在面板**上面**并独占整屏交互：
  面板其实已经画出来了，却被一整层遮罩盖着、点不动，看起来就像「面板根本没出现」。
- 现在弹窗改到面板**下面**绘制，面板永远压在最上层、始终完整可见可点；
  弹窗卡片会主动让到面板右侧，两者互不遮挡。
- 面板热键（默认 `F1`）的语义改成「把面板调出来」：只要还压着弹窗，按下热键就先把弹窗收掉、
  并把面板显示出来，而不是连面板一起关掉。`Esc` 也能直接关掉当前弹窗。

## 修复：某一处绘制出错导致整个面板消失

- 面板、HUD、公告等以前共用同一次 `OnGUI`，任何一处抛异常都会把后面的绘制整块带崩，
  表现同样是「按 `F1` 没反应」。现在每一段单独兜错，互不影响；
  万一真的出错，只会在日志里记一条并标明是哪一段，不再连累其它界面。
