欢迎来到 **Coloring Pixels Tool V2.2.0**！

这一版给「自动绘图」加了一道解锁确认，把 magicui / Aceternity 那套动效语言搬进了 GDI+ 和 IMGUI，
插件面板、安装器、独立助手三端统一外观；同时修掉了此前源码遗留的编译错误，确保能干净地编译出来。

## ✨ 本次更新亮点

### 1. 解锁「自动绘图」现在需要确认

- 「涂色 / 拟人 / 自动化」这三页会直接把结果写进存档，默认上锁；第一次解锁会先弹出风险说明。
- 确认键带 **3 秒冷静期**，确认之后才真正解锁，避免顺手连点直接过掉提示。
- 解锁状态**长期有效**：之后启动游戏不再询问；也可以在「设置 → 自动化与安全」里随时重新上锁。
- 从设置页开启同样会走这道确认，不会绕过。

### 2. 三端 UI 与动效统一升级

无论是游戏里的 F1 面板、一键安装器，还是《涂色大师：像素梦想家》的独立助手 PixelAssist，现在都使用同一套深靛蓝 + 青紫霓虹 + 玻璃卡片设计语言：

- **Aurora Background**：缓慢漂移的柔和光团，只在不打扰操作时提供氛围感。
- **Dot Grid / Grain**：极淡的点阵与噪点纹理，让深色底不那么单调。
- **Shimmer / Glare / ShineBorder**：按钮和卡片会有斜向掠光、悬停反光、沿圆角转圈的高光边。
- **Border Beam / 品牌光条**：卡片顶部有一根渐隐的霓虹线。
- **脉冲光点**：进度条前沿、状态圆点会呼吸发光。
- **按下回弹**：自绘按钮按下时有轻微内缩，松开后回弹。
- **彩纸与数字滚动**：面板完成图片时撒彩纸，百分比数字平滑滚动。

所有动效都是手写 GDI+ / `GUI.matrix`，没有引入任何第三方 UI 库。

### 3. 安装器：更好看 + 更省心

- 主窗口加了 Aurora 背景，卡片和按钮全部霓虹化。
- 进度条现在会平滑滚动、带掠光，完成时还有一层辉光。
- **新增自动关闭**：安装完成后如果检测到游戏主窗口出现，2 秒后自动关闭安装器（60 秒超时），不用手动点 ×。

### 4. 独立助手 PixelAssist 大翻新

- 助手主窗口背景加入 Aurora、点阵、噪点；状态区变成玻璃卡片。
- 所有按钮替换为霓虹自绘按钮，进度条替换为渐变 + Shimmer 版本，新增会呼吸的状态圆点。
- 全屏点击穿透 HUD 改成圆角玻璃卡片：带 ShineBorder、状态呼吸点、霓虹进度条，当前扫描行还会有一道脉冲高亮。

### 5. 面板细节

- 标题栏加 Spotlight，跟随鼠标移动。
- Tab 切换改成滑动指示块 + ShineBorder。
- 内容区入场 Blur Fade，渐隐分隔线，超宽文本自动省略。

### 6. 新增《开发心路历程》

新增文档 [`docs/CHRONICLE.md`](docs/CHRONICLE.md)：按时间线记录每个版本的设计动机与取舍，
从「为什么不直接改存档」到「为什么默认上锁」，README 底部提供入口。

## 🐛 修复

- 修复此前源码遗留的多处编译错误（未定义成员、Harmony 过时 API、未定义字段）。
- 修复安装器 `MainForm.cs` 被截断的编译错误，并把自动关闭相关 `Timer` 限定为 `System.Windows.Forms.Timer` 避免二义性。
- 插件现在能干净地通过 csc / MSBuild / dotnet 三后端编译。

## 💡 小提示

- Coloring Pixels：按 <kbd>F1</kbd> 打开/关闭面板。
- 涂色大师：像素梦想家：先启动游戏，再运行 `PixelAssist\PixelAssist.exe`。
- 命令行党：`ColoringPixelsTool-Setup.exe --silent --all` 会把检测到的游戏一次装完。

## 📦 安装包

| 文件 | 说明 |
|---|---|
| `ColoringPixelsTool-Setup.exe` | 一键安装 / 卸载 / 更新 |
| `ColoringPixelsTool.dll` | 《Coloring Pixels》的 BepInEx 插件 |
| `PixelAssist\PixelAssist.exe` | 《涂色大师：像素梦想家》的独立屏幕扫描助手 |
