欢迎来到 **Coloring Pixels Tool V2.1.0**！

这一版最大的变化是：**它不再只服务一款游戏了。** 同一个安装包现在可以同时认领两款涂色游戏。
另外，Coloring Pixels 的面板被拆成了「自动完成」和「人工辅助」两个大模块，
手速焦虑的朋友终于可以体面地让工具替你把鼠标焊死在画布上。

## ✨ 本次更新亮点

### 1. 新增第二款游戏：《涂色大师：像素梦想家》

安装器启动后会**同时检测两款游戏的目录**，并且**互不影响**——任意一款没识别到，另一款照常安装。

| 游戏 | 加载方式 | 校验特征 |
|---|---|---|
| Coloring Pixels | BepInEx 注入插件（游戏内 F1 面板） | `ColoringPixels.exe` + `Managed\Assembly-CSharp.dll` |
| 涂色大师：像素梦想家 | 独立助手 `PixelAssist.exe` | `PixelCrossStitch.exe` + `GameAssembly.dll` |

> 《涂色大师：像素梦想家》是 IL2CPP 构建，托管插件那套玩法在这款游戏上根本插不进去，
> 所以走的是「外部辅助」路线：一个悬浮覆盖层 + 模拟鼠标，不改游戏本体一个字节。

界面顶部多了游戏切换标签，两款游戏各自记忆目录、安装状态与版本，装哪个卸哪个互不打扰。

### 2. 独立助手 PixelAssist（《涂色大师：像素梦想家》）

- <kbd>F7</kbd> 框选画布 → <kbd>F11</kbd> 框选一个格子自动校准行数与步长 → <kbd>F6</kbd> 开始
- <kbd>F8</kbd> 急停、<kbd>F9</kbd> 试扫当前行、<kbd>F10</kbd> 停止并从第一行重扫、<kbd>F12</kbd> 显示/隐藏范围框
- 全屏**点击穿透**覆盖层：实时画扫描区域、当前扫描线、进度 HUD，绝不挡住你的鼠标
- 参数与区域存在 `%APPDATA%\PixelAssist`，升级安装包不会把配置一起升走

### 3. 面板拆成「自动完成」＋「人工辅助」

- **自动完成**：原来的全部功能原封不动搬过来（涂色、拟人、自动化、辅助、解锁、设置、调试）。
- **人工辅助**：按设计文档实现。屏幕扫描辅助区域用「四角 + 弯边」模型描述，可以逐角拖动微调；
  扫描行数 / 速度 / 采样步长 / 行间停顿 / 蛇形 / 边缘内缩 / 启动延时 / 按住按键 /
  自动停止时长 / 每 N 行自动换色 / 失败半径 / 人工介入检测 / 越界收敛都能调，参数可以存成预设。

### 4. 面板终于会自适应缩放了

面板整体按屏幕分辨率和用户倍率缩放（配置项 `PanelScale`，填 `0` 就是自适应），
超宽文本自动截断成省略号。小屏用户不用再忍受文字叠罗汉了。

### 5. 等级涨得慢了，但更有意思了

- 经验增长大幅放缓，挂机收益远低于真人操作。
- 手动点击次数、手动涂色率纳入经验判定——真的自己涂才涨得快。
- 每个等级区间都有专属称号，升级会弹 Toast。

## 🐛 修复

- 修复非 1080p 分辨率下面板命中区域与实际控件错位的问题。
- 安装器：安装 / 卸载 / 提权重启全部按当前选中的游戏执行，不再张冠李戴。
- 安装器：不再只看文件长度判断「已是最新」，改为逐字节比对，避免新插件因长度相同而更新不上。

## 💡 小提示

- Coloring Pixels：按 <kbd>F1</kbd> 打开/关闭面板。
- 涂色大师：像素梦想家：先启动游戏，再运行 `PixelAssist\PixelAssist.exe`。
- 命令行党：`ColoringPixelsTool-Setup.exe --silent --all` 会把检测到的游戏一次装完。

## 📦 安装包

| 文件 | 说明 |
|---|---|
| `ColoringPixelsTool-Setup-v2.1.0.exe` | 一键安装器（两款游戏通用） |
| `SHA256SUMS.txt` | 校验文件 |

祝涂色愉快！

—— Coloring Pixels Tool 团队（其实就一个人但团队听起来比较厉害）
