欢迎来到 **Coloring Pixels Tool V2.2.1**！

这一版把两份主要文档补齐到了当前功能，并修掉了一个一直存在的小毛病：
面板上人工辅助的按钮和提示里还写着独立助手的按键，和插件实际的热键对不上。

## 🐛 本次修复

### 1. 人工辅助的按键提示不再对不上

插件里 <kbd>F1</kbd>~<kbd>F6</kbd> 已经被面板、一键涂完、拟人涂色、清空、保存、颜色高亮占用，
所以人工辅助整体后移一位（默认 <kbd>F7</kbd> 开始、<kbd>F8</kbd> 框选、<kbd>F9</kbd> 停止）。
但「扫描」页的按钮、「区域」页的说明和新手指引第 3 页一直写的是独立助手的 <kbd>F6</kbd> / <kbd>F8</kbd>，容易让人按错。

现在这些文案统一改为**读取你当前的快捷键配置**：

- 改键之后，按钮上的提示会立刻跟着变；
- 覆盖层开关没绑按键时，会提示「可在设置里绑定」，不再显示「未设置」；
- 涉及「扫描」页、「区域」页、新手指引三处，全部已同步。

### 2. README 徽章指向不存在的仓库

Release / Build 两个徽章里的仓库名少了一个连字符（`zlwzk/ColoringPixelsTool`），一直点不开，现已修正。

## 📖 文档补齐

### `docs/USAGE.md` 全面重写

之前这份手册还停在只有一款游戏的时代。现在补齐了：

- 两款游戏的接入方式与安装 / 卸载差异（含 `PixelAssist` 的独立卸载说明）；
- 插件 F1–F12 + <kbd>Q</kbd>、独立助手 F6–F12 两套完整热键表；
- 面板「自动完成」（首页 / 涂色 / 拟人 / 自动化 / 辅助 / 解锁 / 设置 / 调试）与
  「人工辅助」（扫描 / 区域 / 参数 / 预设 / 助手 / 设置 / 调试）两大模块的逐页说明；
- 「自动绘图」默认上锁与解锁流程（含 3 秒冷静期）；
- 独立助手窗口的全部参数、覆盖层行为与配置目录；
- 12 个配置分组（`0-通用` ~ `B-反馈`）的完整键值表。

### `README.md` 同步到当前版本

双游戏简介与三组件表、特性清单、F1–F12 快捷键、`--game` / `--all` 参数，
常见问题新增「为什么『涂色 / 拟人 / 自动化』点不进去」「第二款游戏该怎么用」。

### `docs/BUILDING.md` 小修

修正了 `publish.ps1` 的错误参数写法（`-Release` 并不存在，正确做法是默认发版、用 `-NoRelease` 只提交），
以及写错的仓库地址。

## 💡 小提示

- Coloring Pixels：按 <kbd>F1</kbd> 打开/关闭面板，<kbd>F7</kbd> 开始人工辅助。
- 涂色大师：像素梦想家：先启动游戏，再运行 `PixelAssist\PixelAssist.exe`，按 <kbd>F7</kbd> 框选、<kbd>F6</kbd> 开始。
- 命令行党：`ColoringPixelsTool-Setup.exe --silent --all` 会把检测到的游戏一次装完。

## 📦 安装包

| 文件 | 说明 |
|---|---|
| `ColoringPixelsTool-Setup.exe` | 一键安装 / 卸载 / 更新 |
| `ColoringPixelsTool.dll` | 《Coloring Pixels》的 BepInEx 插件 |
| `PixelAssist\PixelAssist.exe` | 《涂色大师：像素梦想家》的独立屏幕扫描助手 |
