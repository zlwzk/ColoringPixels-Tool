欢迎回来，**Coloring Pixels Tool V2.2.12**！

这版把《涂色大师：像素梦想家》的独立助手彻底补全了——以前它只有「人工辅助」半个模块，
现在自动绘图、等级经验、图案预览和遮罩 HUD 都齐了，两个游戏共用同一套等级与进度。

## 🎨 涂色大师助手：终于不用全程自己点了

独立助手 `PixelAssist.exe` 现在和 Coloring Pixels 插件的功能基本对齐：

- **六页签面板**：首页、自动绘图、人工辅助、预览、等级、设置；
- **自动绘图**：直接读游戏存档里的目标颜色，按颜色分组自动点击/拖拽填涂；
- **调色板自动识别**：框一下游戏里的调色板，助手自己找出色块中心，涂下一组颜色前自动点切换；
- **只涂当前颜色**：不想自动切颜色的话，勾上「只涂当前颜色」，它会只填当前选中的那一色；
- **拟人参数**：手速、笔触长度、停笔概率、手滑概率都能调，想快就快、想憨就憨；
- **图案预览**：不截图，直接拿存档里的目标颜色拼出整幅图，已涂真彩、未涂暗底稿，滚轮缩放、拖拽平移；
- **遮罩 HUD**：自动绘图时高亮「正要涂的格子」，并在画面上方显示当前颜色、已涂/剩余进度。

启动方式是安装器自动配好的：

- <kbd>F7</kbd> 框选游戏里的画布区域；
- <kbd>F5</kbd> 框选调色板区域；
- <kbd>F6</kbd> 开始 / 暂停自动绘图；
- <kbd>F8</kbd> 急停。

## 🔄 两个游戏共用一个等级系统

Coloring Pixels 的插件端和涂色大师的独立助手端，现在读的是同一份用户资料：

- 两条经验线（人工辅助 / 自动绘图）双向同步；
- 等级、称号、完成图数、涂色格数、最大完成图等统计两边合并取最大，先写后写都不丢；
- 存档位置仍在 `%APPDATA%\ColoringPixelsTool`，重装插件、验证游戏文件、切换游戏都不会把等级带走。

## 🛡️ 后台绘图更稳

- 自动绘图跑在独立线程，不再用阻塞 `Invoke` 回 UI，避免点「停止」时卡三秒；
- 共享存档写入加了互斥锁，临时文件名也带上线程序号，UI 线程定时存盘和后台线程升级存盘不再互相踩；
- 存档刷新改成「文件时间戳变了才重新解析」，不再每两秒把下拉框整个重建一遍。

## ⚙️ 其它顺手修的

- 新增「纵向翻转」选项，万一游戏存档里的行序和屏幕方向相反，勾一下就能让落点和预览都对齐；
- 自动涂完整张图时，现在会正确把「完成图片」的经验加到「自动绘图」轨；
- 等级页会实时显示两条经验线的进度、等级、称号和下一级提示。
